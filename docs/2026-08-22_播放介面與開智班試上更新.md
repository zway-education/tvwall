# 2026-08-22 電視牆更新紀錄

## 使用者要求

- 多張畫面右下角不再顯示「已暫停」或播放進度。
- 原 SEL「小小專注實踐家」改為開智班試上課程。
- 場次改為 2026/8/29、9/5、9/12，時間 14:00–14:50。
- 舊海報版面不可只用遮罩勉強覆蓋，需重新調整圖片與排版。
- 使用者認為第一版重新排版仍不好看，要求改由網頁版 GPT 生成。
- 網頁版 GPT 第一版色彩飽和度過高；保留版面結構，再將橘、黃、青綠與紫色的飽和度降低約 30%。
- 低飽和度版本方向可接受，但電視牆顯示仍偏暗；維持低彩度配色，將整體亮度提高約 12～15%。

## 原因診斷

- 主頁程式本身已沒有暫停控制，但 `new-1.png` 至 `new-6.png` 的舊截圖像素內已烙入進度點與「已暫停」。
- `vertical.html` 仍保留可見控制列與空白鍵暫停快捷鍵。
- 舊活動海報是點陣圖片，標題與舊活動資訊無法只靠文字替換完整更新。

## 完成調整

- 六張舊 SEL 截圖在網頁端加入底部乾淨遮罩，移除烙入圖片的播放狀態。
- 直式頁隱藏播放控制列與進度列，並移除空白鍵暫停快捷鍵。
- 網頁版 GPT 生成全新的 2:3 探索主視覺，依序重製為低飽和度版本及較明亮版本。
- 在 GPT 保留的白色區域置入真實報名 QR Code，未使用模型生成的假 QR Code。
- 網站活動頁改用 `focus-event-poster-20260829-gpt.png`，並以完整 2:3 比例顯示，不裁掉上下內容。
- 右側同步改為「開智班試上課程」與三個最新場次。
- 「專注練習／練習把」改為「專注投入／逐步把」，符合現行 K12 文字規範。

## 現行來源

- 網頁版 GPT 明亮低飽和度海報原圖：`C:\Users\KHUser\Downloads\244c9c55-fa92-465d-9843-fa770456c466.png`
- 網站正式海報：`focus-event-poster-20260829-gpt.png`
- 真實報名 QR Code：`focus-event-qr.png`

## 驗證

- PowerShell 展開全部 `tests\*.test.mjs` 後執行 `node --test`：28 項通過。
- 本機以 `?start=15` 開啟實際 16:9 電視牆頁，已確認低飽和度海報完整顯示，日期、時間、地點及 QR Code 可見。
- 直式頁實際 computed style：`.controls = none`、`.progress = none`。
